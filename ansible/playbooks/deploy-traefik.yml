---
- name: Deploy Traefik Reverse Proxy
  hosts: backend
  become: yes
  vars_files:
    - ../vars/common.yml
    - ../vars/{{ environment | default('dev') }}.yml
  
  roles:
    - traefik

  vars:
    traefik_version: "3.0.0"
